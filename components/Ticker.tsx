import { getSiteConfig, type Locale } from '@/config/site';
export default function Ticker({ locale = 'zh' }: { locale?: Locale }) { const { home } = getSiteConfig(locale); return <div className="ticker" aria-hidden="true"><div className="ticker-line">{Array.from({ length: 2 }).flatMap((_, index) => home.ticker.map((item, itemIndex) => <span className="ticker-item" key={`${index}-${itemIndex}`}>{item}</span>))}</div></div>; }